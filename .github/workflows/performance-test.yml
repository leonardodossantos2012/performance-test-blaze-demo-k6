name: Performance Tests

on:
  workflow_dispatch:
    inputs:
      test_type:
        description: 'Tipo de teste a executar'
        required: true
        type: choice
        options:
          - load
          - spike
        default: 'load'

jobs:
  performance-test:
    name: Flight Booking Performance Test
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install k6
        run: |
          sudo gpg -k
          sudo gpg --no-default-keyring --keyring /usr/share/keyrings/k6-archive-keyring.gpg --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys C5AD17C747E3415A3642D57D77C6C491D6AC1D69
          echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt-get update
          sudo apt-get install k6

      - name: Run performance test
        id: run_test
        continue-on-error: true
        run: |
          mkdir -p results
          
          # Selecionar o script de teste baseado no tipo escolhido
          TEST_TYPE="${{ github.event.inputs.test_type }}"
          TEST_SCRIPT=""
          
          case "$TEST_TYPE" in
            "load")
              TEST_SCRIPT="tests/flight-booking-load-test.js"
              echo "Running Load Test"
              ;;
            "spike")
              TEST_SCRIPT="tests/flight-booking-spike-test.js"
              echo "Running Spike Test"
              ;;
            *)
              echo "Invalid test type: $TEST_TYPE"
              exit 1
              ;;
          esac
          
          k6 run \
            --out json=results/k6-results.json \
            --out csv=results/k6-results.csv \
            $TEST_SCRIPT
          echo "exit_code=$?" >> $GITHUB_OUTPUT
          echo "test_type=$TEST_TYPE" >> $GITHUB_OUTPUT
          echo "test_script=$TEST_SCRIPT" >> $GITHUB_OUTPUT

      - name: Move HTML report to results directory
        if: always()
        run: |
          if [ -f report.html ]; then
            mv report.html results/report.html
            echo "✅ HTML report moved to results/report.html"
            ls -lh results/report.html
          else
            echo "⚠️ HTML report not found, but continuing..."
          fi

      - name: Verify HTML report generation
        if: always()
        run: |
          if [ -f results/report.html ]; then
            echo "✅ HTML report verified in results directory"
          else
            echo "⚠️ HTML report not found in results directory"
          fi

      - name: Upload test results as artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: k6-performance-results
          path: |
            results/
          retention-days: 30
